# .travis.yml

# Use Ubuntu 20.04 LTS as the build environment
#os: linux
#dist: focal
language: minimal

branches:
  only:
    - main
# Install dependencies
install:
  - echo "Installing dependencies here"

# Run tests
script:
  - echo "Run tests here"

# Deploy script
deploy:
  provider: script
  skip_cleanup: true
  script: /bin/bash script.sh
  on:
    branch: master
