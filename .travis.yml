# .travis.yml

# Use Ubuntu 20.04 LTS as the build environment
#os: linux
#dist: focal
language: minimal

branches:
  only:
    - main
# Install dependencies
install:
  - echo "Installing dependencies here"

# Run tests
script:
  - echo "Run tests here"

# Deploy script
deploy:
  provider: script
  skip_cleanup: true
  script: /deploy.sh  # Adjust the path to your script
  on:
    branch: master
